import {
    StyleSheet
} from 'react-native';
export default StyleSheet.create({
    continner: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1,
    },
    input: {
        width: "60%",
        marginTop: 20,
    },
    button: {
        width: "60%",
        marginTop: 20,
    },
});